[project]
name = "ai-inference-service"
version = "0.1.0"
description = "Production-style AI inference service"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]",
    "uvicorn[standard]",
]

[tool.fastapi]
entrypoint = "app.main:app"